# ===========================
# Core Libraries
# ===========================

# Sentence Transformers: for generating semantic sentence embeddings
# Model example: all-MiniLM-L6-v2
sentence-transformers==2.2.2

# scikit-learn: for computing similarity metrics (cosine similarity, etc.)
# Also useful for machine learning utilities if needed
scikit-learn==1.2.2

# NumPy: efficient numerical operations and vector computations
# Required by sentence-transformers and scikit-learn
numpy==1.25.0

# Pandas: data manipulation and CSV handling
# Load, clean, and process datasets easily
pandas==2.1.0

# ===========================
# Optional / Development Libraries
# ===========================

# Jupyter Notebook: for interactive experimentation and testing
# Recommended if running notebooks for testing or demonstrations
jupyter==1.0.0

# FAISS (optional): library for fast similarity search on large embedding datasets
# Only required if you plan to scale retrieval to very large datasets
faiss-cpu==1.7.4

# tqdm: for progress bars during embedding generation or data processing
# Helpful for large datasets to visualize progress
tqdm==4.66.1

# ===========================
# Notes
# ===========================
# 1. Python version recommended: 3.8+
# 2. To install all dependencies:
#      pip install -r requirements.txt
# 3. If using FAISS for GPU acceleration, install faiss-gpu instead of faiss-cpu
# 4. Internet connection is required for the first run of sentence-transformers
#    to download pretrained models. Subsequent runs will use cached models.
